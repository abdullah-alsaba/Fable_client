import AdminDashboard from "@/Components/Dashboard/AdminDashboard";

export const metadata = {
  title: "Admin Dashboard – Fable Ebook Platform",
  description: "Platform analytics, sales charts with Recharts, manage users, manage all ebooks, and view all transactions.",
};

export default function AdminDashboardPage() {
  return <AdminDashboard />;
}
